/**
 * An object or array containing strings, numbers, bigints or any combination of them
 * @typedef {(Object.<(string|number|bigint)>|Array.<(string|number|bigint)>)} valueCollection
 */

/**
 * An object containing <code>breakpoints</code>
 * @typedef {Object} theme
 * @property {valueCollection} breakpoints - An object or array of CSS <code>min-width</code> values or numbers
 */

/**
 * An object containing <code>margin</code> and <code>theme</code>
 * @typedef {Object} props
 * @property {theme} theme - An object containing a <code>breakpoints</code> object or array
 */
